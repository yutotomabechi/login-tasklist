<h1>id = 
<%= @task.id %>
 の詳細ページ</h1>

<table class="table table-bordered">
  <tr>
    <th>id</th>
    <td><%= @task.id %></td>
  </tr>
  <tr>
    <th>タスク</th>
    <td><%= @task.content %></td>
  </tr>
  <tr>
    <th>ステータス</th>
    <td><%= @task.status %></td>
  </tr>
</table>


<%= link_to 'このタスクを編集する', edit_task_path(@task), class: "btn btn-primary"%>


<%= link_to 'このタスクを削除する', @task, method: :delete, data: { confirm: '本当に削除してよろしいですか？', class: 'btn btn-danger' } %>

<%= link_to '一覧に戻る', root_path, class: "btn btn-primary"%>

